export interface ProductCass{
    kategorija: string,
    tip: string,
    naziv: string,
    cena: number,
    ocena: number,
    slika: string,
    opis: string,
    proizvodjac: string,
    popust: number //u % popust
}