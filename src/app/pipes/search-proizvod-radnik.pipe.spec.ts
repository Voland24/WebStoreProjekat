import { SearchProizvodRadnikPipe } from './search-proizvod-radnik.pipe';

describe('SearchProizvodRadnikPipe', () => {
  it('create an instance', () => {
    const pipe = new SearchProizvodRadnikPipe();
    expect(pipe).toBeTruthy();
  });
});
